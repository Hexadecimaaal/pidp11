; no serial devices, network connections, or host configuration are used
set console halt="warning-bounds-probe"
show console halt
set console nohalt
help set console
; the build check runs in its isolated source tree
dir tests/*.ini
echo CONSOLE_SMOKE_PASSED
quit
